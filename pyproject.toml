[project]
name = "FinanceTracker"
version = "1.1.2"
description = "A simple finance tracking app for managing budgets, expenses, and income records."
readme = "README.md"
requires-python = ">=3.13"

authors = [
    { name = "MrNichosik" }
]

dependencies = [
    "prompt-toolkit>=3.0"
]